# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on PR uat
'on': 
  pull_request:
    branches: 
      - uat

jobs:
  build_and_preview:
    if: '${{ github.event.pull_request.head.repo.full_name == github.repository }}'
    runs-on: ubuntu-latest
    environment: uat
    # env:
    #   VITE_FIREBASE_API_KEY: ${{ secrets.FIREBASE_API_KEY }}
    #   VITE_FIREBASE_AUTH_DOMAIN: ${{ secrets.FIREBASE_AUTH_DOMAIN }}
    #   VITE_FIREBASE_PROJECT_ID: ${{ secrets.FIREBASE_PROJECT_ID }}
    #   VITE_FIREBASE_STORAGE_BUCKET: ${{ secrets.FIREBASE_STORAGE_BUCKET }}
    #   VITE_FIREBASE_MESSAGE_SEND_ID: ${{ secrets.FIREBASE_MESSAGE_SEND_ID }}
    #   VITE_FIREBASE_APP_ID: ${{ secrets.FIREBASE_APP_ID }}
    #   VITE_MEASUREMENT_ID: ${{ secrets.MEASUREMENT_ID }}
    #   VITE_MESSAGE_ID: ${{ secrets.MESSAGE_ID }}
    #   VITE_BACKEND_URL: ${{ secrets.BACKEND_URL }}
    steps:
      - uses: actions/checkout@v3
      - run: npm i && npm run build
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_APP_TODO_47999 }}'
        
